# LowCast Environment Variables

# Enable/Disable App Monitor (logs & performance metrics)
# Set to "true" to enable the App Monitor feature in the UI
# Default: disabled (when not set or set to any value other than "true")
VITE_ENABLE_APP_MONITOR=true
